apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: caps-command
  labels:
    knative.dev/type: "function"
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: github.com/botless/commands/cmd/core/
            env:
            - name: TARGET
              value: "http://default-broker.default.svc.cluster.local"
            - name: STRICT_TYPE
              value: "botless.bot.command.caps"
---
apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: caps-command
spec:
  filter:
    sourceAndType:
      type: botless.bot.command.caps
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1alpha1
      kind: Service
      name: caps-command
