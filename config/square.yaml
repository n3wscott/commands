apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: square-command
  labels:
    knative.dev/type: "function"
    eventing.knative.dev/autotrigger: "true"
  annotations:
    trigger.eventing.knative.dev/filter: |
      [{"type":"botless.bot.command.square"}]
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: github.com/botless/commands/cmd/core/
            env:
            - name: STRICT_TYPE
              value: "botless.bot.command.square"
