apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: default-commands
  labels:
    knative.dev/type: "function"
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: github.com/botless/commands/cmd/default/
            env:
            - name: TARGET
              value: "http://slack-out-channel-7ls72.default.svc.cluster.local/" # <---------------   TODO: update this.
---
apiVersion: eventing.knative.dev/v1alpha1
kind: Subscription
metadata:
  name: default-commands
spec:
  channel:
    apiVersion: eventing.knative.dev/v1alpha1
    kind: Channel
    name: parser-out
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1alpha1
      kind: Service
      name: default-commands
